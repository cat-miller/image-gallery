import styled from 'styled-components';

const StyledSidebar = styled.div`
  display: grid;
  align-content: start;
  padding-left: 1rem;
  font-size: 14px;
  gap: 1rem;
`
export default StyledSidebar;